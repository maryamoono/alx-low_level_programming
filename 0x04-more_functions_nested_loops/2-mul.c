#include <stdio.h>
/**
 * _mul - cheak this code
 *
 * @a: an interger
 * @b: an integer
 *
 * Return: Always 0 (success)
 */
int _mul(int a, int b)
{
	int a;
	int b;

	printf("%d\n", _mul( int a, int b));
	return (0);
}
