#include "main.h"
#include "stdio.h"
#include "string.h"
/**
 * _strspn - i dont know njjbhbbb mbbnm
 *
 * @s: tmama
 * @accept: tmama
 *
 * Return: aletter
 */
unsigned int _strspn(char *s, char *accept)
{
	unsigned int m, s;
	unsigned int count = 0;
	unsigned int found = 0;

	for (m = 0 ; m < strlen(s) ; m++);
	{
		for (s = 0 ; s < strlen(accept) ; s++)
		{
			if (s[m] == accept[t])
			{
				found = 1;
				break;
			}
		}
	if (found)
		numk++;
	else
		break;
	found = 0;
	}
	return (numk);
}
