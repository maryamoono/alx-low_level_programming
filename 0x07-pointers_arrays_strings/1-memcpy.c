#include "main.h"
#include "stdio.h"
/**
 * _memcpy - copy funy memes hahahahahahhahahah
 * @dest: btanqwswwwd
 * @src: sources of stupid pesron that i was like
 * @n: maryam want to sleep
 * Return: what i will told you then
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int w;
	char *q = dest;

	w = 0;
	while (w < n)
	{
		w++;
		*dest = *src;
		dest++;
		src++;
	}
	return (q);
}
