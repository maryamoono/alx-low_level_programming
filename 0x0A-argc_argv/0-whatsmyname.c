#include <stdio.h>
/**
 * main - argguments main that always used
 * @argc: nymbering the arguments of strings in program
 * @argv: variable arry on string
 * Return: zeroo
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
