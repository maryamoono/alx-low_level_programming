#include <stdio.h>
/**
 * main - nndandkn
 * @argc: kiujnhby
 *
 * @argv: mlawj
 * Return: nuukk
 */
int main(int argc, char *argv[])
{
	int x;

	for (x = 0 ; x < argc ; x++)
	{
		printf("%s", argv[x]);
		if (x < argc)
		{
			printf("\n");
		}
	}
	return (0);
}
